# Nginx service status: 1=active, 0=inactive
UserParameter=nginx.status,/etc/zabbix/scripts/check_nginx.sh

# Nginx active connections from stub_status
UserParameter=nginx.active.connections,/etc/zabbix/scripts/check_nginx_active.sh

